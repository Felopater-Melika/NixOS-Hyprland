# Machine-local Hyprland environment overrides.
# Keep this file in the repo so a fresh install does not source a missing path.
# Add extra `env = KEY,VALUE` lines here only when the system-specific values
# cannot live in Home Manager.
